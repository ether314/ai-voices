# Voice profile (zero-shot clone — no fine-tune)

- `me.mp3` — original upload (~59s)
- `me.wav` — best ~10s mono clip (auto-picked); **default voice** for `/tts`

API uses `/voices/me.wav` when you omit `audio_prompt`.
